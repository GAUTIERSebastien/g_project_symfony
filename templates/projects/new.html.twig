{# {% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-5">
                {{ form_start(form, {'attr': {'class': 'form-group'}}) }}
                    {{ form_widget(form, {'attr': {'class': 'form-control mb-3'}}) }}
                    <button class="btn btn-primary d-block mx-auto">{{ button_label|default('Ajouter') }}</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %} #}
